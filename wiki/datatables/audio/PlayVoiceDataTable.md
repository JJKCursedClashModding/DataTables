# PlayVoiceDataTable

**Source:** `input_json/PlayVoiceDataTable.json`.

**Row struct (from export):** `Class'GameDataTableRow_PlayVoice'`

## Fields

Each line is a row field path (nested structs use dot notation; `[]` marks array-of-struct). The literal **Unknown** is the official description placeholder; the bracketed line is an AI guess from the field name only.

- **DelayTime** — Unknown (AI - Possible description: Duration in seconds unless engine uses normalized time.)
- **ID** — Unknown (AI - Possible description: Primary identifier for this row; used as DataTable row name and for lookups.)
- **Id_Voice** — Unknown (AI - Possible description: Foreign key to another DataTable or asset row; suffix suggests voice.)
- **PlayIndexOrder** — Unknown (AI - Possible description: Inferred from name: play index order. Verify in blueprints or binary if accuracy matters.)
- **PlayVoiceType** — Unknown (AI - Possible description: Enum or category selector driving gameplay branches.)
- **UserProgressFlag** — Unknown (AI - Possible description: Inferred from name: user progress flag. Verify in blueprints or binary if accuracy matters.)
- **Weights_VoiceLottery** — Unknown (AI - Possible description: Inferred from name: weights voice lottery. Verify in blueprints or binary if accuracy matters.)
