# CameraShakeDataTable

**Source:** `input_json/CameraShakeDataTable.json`.

**Row struct (from export):** `Class'GameDataTableRow_CameraShake'`

## Fields

Each line is a row field path (nested structs use dot notation; `[]` marks array-of-struct). The literal **Unknown** is the official description placeholder; the bracketed line is an AI guess from the field name only.

- **ID** — Unknown (AI - Possible description: Primary identifier for this row; used as DataTable row name and for lookups.)
- **OscillationBlendInTime** — Unknown (AI - Possible description: Duration in seconds unless engine uses normalized time.)
- **OscillationBlendOutTime** — Unknown (AI - Possible description: Duration in seconds unless engine uses normalized time.)
- **OscillationDuration** — Unknown (AI - Possible description: Inferred from name: oscillation duration. Verify in blueprints or binary if accuracy matters.)
- **RotOscillation_Pitch_Amplitude** — Unknown (AI - Possible description: Inferred from name: rot oscillation pitch amplitude. Verify in blueprints or binary if accuracy matters.)
- **RotOscillation_Pitch_Frequency** — Unknown (AI - Possible description: Inferred from name: rot oscillation pitch frequency. Verify in blueprints or binary if accuracy matters.)
- **RotOscillation_Yaw_Amplitude** — Unknown (AI - Possible description: Inferred from name: rot oscillation yaw amplitude. Verify in blueprints or binary if accuracy matters.)
- **RotOscillation_Yaw_Frequency** — Unknown (AI - Possible description: Inferred from name: rot oscillation yaw frequency. Verify in blueprints or binary if accuracy matters.)
