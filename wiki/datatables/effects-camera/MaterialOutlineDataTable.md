# MaterialOutlineDataTable

**Source:** `input_json/MaterialOutlineDataTable.json`.

**Row struct (from export):** `Class'GameDataTableRow_MaterialOutline'`

## Fields

Each line is a row field path (nested structs use dot notation; `[]` marks array-of-struct). The literal **Unknown** is the official description placeholder; the bracketed line is an AI guess from the field name only.

- **FadeInTime** — Unknown (AI - Possible description: Duration in seconds unless engine uses normalized time.)
- **FadeOutTime** — Unknown (AI - Possible description: Duration in seconds unless engine uses normalized time.)
- **ID** — Unknown (AI - Possible description: Primary identifier for this row; used as DataTable row name and for lookups.)
- **OutlineEmissiveColor.X** — Unknown (AI - Possible description: Inferred from name: x. Verify in blueprints or binary if accuracy matters.)
- **OutlineEmissiveColor.Y** — Unknown (AI - Possible description: Inferred from name: y. Verify in blueprints or binary if accuracy matters.)
- **OutlineEmissiveColor.Z** — Unknown (AI - Possible description: Inferred from name: z. Verify in blueprints or binary if accuracy matters.)
- **OutlineScale** — Unknown (AI - Possible description: Uniform scale factor for an effect, model, or value.)
