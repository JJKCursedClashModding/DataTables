# SequencerLightDataTable

**Source:** `input_json/SequencerLightDataTable.json`.

**Row struct (from export):** `Class'GameDataTableRow_SequencerLight'`

## Fields

Each line is a row field path (nested structs use dot notation; `[]` marks array-of-struct). The literal **Unknown** is the official description placeholder; the bracketed line is an AI guess from the field name only.

- **bDirectionalLightCastShadows** — Unknown (AI - Possible description: Boolean flag: when true, enables or applies directional light cast shadows.)
- **ID** — Unknown (AI - Possible description: Primary identifier for this row; used as DataTable row name and for lookups.)
- **Id_Map** — Unknown (AI - Possible description: Foreign key to another DataTable or asset row; suffix suggests map.)
- **LevelSequenceFileName** — Unknown (AI - Possible description: Inferred from name: level sequence file name. Verify in blueprints or binary if accuracy matters.)
