# CreditDataTable

**Source:** `input_json/CreditDataTable.json`.

**Row struct (from export):** `Class'GameDataTableRow_Credit'`

## Fields

Each line is a row field path (nested structs use dot notation; `[]` marks array-of-struct). The literal **Unknown** is the official description placeholder; the bracketed line is an AI guess from the field name only.

- **ContentType** — Unknown (AI - Possible description: Enum or category selector driving gameplay branches.)
- **FontSize** — Unknown (AI - Possible description: Inferred from name: font size. Verify in blueprints or binary if accuracy matters.)
- **ID** — Unknown (AI - Possible description: Primary identifier for this row; used as DataTable row name and for lookups.)
- **IsDlc1** — Unknown (AI - Possible description: Inferred from name: is dlc1. Verify in blueprints or binary if accuracy matters.)
- **IsDlc2** — Unknown (AI - Possible description: Inferred from name: is dlc2. Verify in blueprints or binary if accuracy matters.)
- **IsMain** — Unknown (AI - Possible description: Inferred from name: is main. Verify in blueprints or binary if accuracy matters.)
- **IsShort** — Unknown (AI - Possible description: Inferred from name: is short. Verify in blueprints or binary if accuracy matters.)
- **Justify** — Unknown (AI - Possible description: Inferred from name: justify. Verify in blueprints or binary if accuracy matters.)
- **LetterSpacing** — Unknown (AI - Possible description: Inferred from name: letter spacing. Verify in blueprints or binary if accuracy matters.)
- **LineHeightPercentage** — Unknown (AI - Possible description: Inferred from name: line height percentage. Verify in blueprints or binary if accuracy matters.)
- **LineNumber** — Unknown (AI - Possible description: Inferred from name: line number. Verify in blueprints or binary if accuracy matters.)
- **Text** — Unknown (AI - Possible description: Inferred from name: text. Verify in blueprints or binary if accuracy matters.)
