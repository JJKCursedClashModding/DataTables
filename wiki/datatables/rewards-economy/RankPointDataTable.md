# RankPointDataTable

**Source:** `input_json/RankPointDataTable.json`.

**Row struct (from export):** `Class'GameDataTableRow_RankPoint'`

## Fields

Each line is a row field path (nested structs use dot notation; `[]` marks array-of-struct). The literal **Unknown** is the official description placeholder; the bracketed line is an AI guess from the field name only.

- **ID** — Unknown (AI - Possible description: Primary identifier for this row; used as DataTable row name and for lookups.)
- **Id_GlobalThreshold** — Unknown (AI - Possible description: Foreign key to another DataTable or asset row; suffix suggests global threshold.)
- **LoseLowerLimit** — Unknown (AI - Possible description: Inferred from name: lose lower limit. Verify in blueprints or binary if accuracy matters.)
- **RankPointClass** — Unknown (AI - Possible description: Inferred from name: rank point class. Verify in blueprints or binary if accuracy matters.)
- **RankPointGrade** — Unknown (AI - Possible description: Inferred from name: rank point grade. Verify in blueprints or binary if accuracy matters.)
