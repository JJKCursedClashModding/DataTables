# EquipmentItemEffectDataTable

**Source:** `input_json/EquipmentItemEffectDataTable.json`.

**Row struct (from export):** `Class'GameDataTableRow_EquipmentItemEffect'`

## Fields

Each line is a row field path (nested structs use dot notation; `[]` marks array-of-struct). The literal **Unknown** is the official description placeholder; the bracketed line is an AI guess from the field name only.

- **EffectType** — Unknown (AI - Possible description: Enum or category selector driving gameplay branches.)
- **EffectValue** — Unknown (AI - Possible description: Inferred from name: effect value. Verify in blueprints or binary if accuracy matters.)
- **ID** — Unknown (AI - Possible description: Primary identifier for this row; used as DataTable row name and for lookups.)
- **Id_BattleText** — Unknown (AI - Possible description: Foreign key to another DataTable or asset row; suffix suggests battle text.)
- **Id_BuffDebuff** — Unknown (AI - Possible description: BuffDebuffDataTable row to apply or remove status.)
