# EquipmentItemDataTable

**Source:** `input_json/EquipmentItemDataTable.json`.

**Row struct (from export):** `Class'GameDataTableRow_EquipmentItem'`

## Fields

Each line is a row field path (nested structs use dot notation; `[]` marks array-of-struct). The literal **Unknown** is the official description placeholder; the bracketed line is an AI guess from the field name only.

- **Grade** — Unknown (AI - Possible description: Inferred from name: grade. Verify in blueprints or binary if accuracy matters.)
- **ID** — Unknown (AI - Possible description: Primary identifier for this row; used as DataTable row name and for lookups.)
- **Id_EquipmentItemEffect** — Unknown (AI - Possible description: Foreign key to another DataTable or asset row; suffix suggests equipment item effect.)
