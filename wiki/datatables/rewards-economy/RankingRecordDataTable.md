# RankingRecordDataTable

**Source:** `input_json/RankingRecordDataTable.json`.

**Row struct (from export):** `Class'GameDataTableRow_RankingRecord'`

## Fields

Each line is a row field path (nested structs use dot notation; `[]` marks array-of-struct). The literal **Unknown** is the official description placeholder; the bracketed line is an AI guess from the field name only.

- **ID** — Unknown (AI - Possible description: Primary identifier for this row; used as DataTable row name and for lookups.)
- **Priority** — Unknown (AI - Possible description: Inferred from name: priority. Verify in blueprints or binary if accuracy matters.)
- **PrizeThreshold** — Unknown (AI - Possible description: Inferred from name: prize threshold. Verify in blueprints or binary if accuracy matters.)
- **RankingRecord** — Unknown (AI - Possible description: Inferred from name: ranking record. Verify in blueprints or binary if accuracy matters.)
