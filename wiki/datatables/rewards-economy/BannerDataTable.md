# BannerDataTable

**Source:** `input_json/BannerDataTable.json`.

**Row struct (from export):** `Class'GameDataTableRow_Banner'`

## Fields

Each line is a row field path (nested structs use dot notation; `[]` marks array-of-struct). The literal **Unknown** is the official description placeholder; the bracketed line is an AI guess from the field name only.

- **BannerTextureName** — Unknown (AI - Possible description: Inferred from name: banner texture name. Verify in blueprints or binary if accuracy matters.)
- **ID** — Unknown (AI - Possible description: Primary identifier for this row; used as DataTable row name and for lookups.)
- **Id_BannerMessage** — Unknown (AI - Possible description: Foreign key to another DataTable or asset row; suffix suggests banner message.)
- **SmallBannerTextureName** — Unknown (AI - Possible description: Inferred from name: small banner texture name. Verify in blueprints or binary if accuracy matters.)
- **UserProgressFlag** — Unknown (AI - Possible description: Inferred from name: user progress flag. Verify in blueprints or binary if accuracy matters.)
