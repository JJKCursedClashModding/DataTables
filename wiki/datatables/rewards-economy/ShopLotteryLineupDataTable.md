# ShopLotteryLineupDataTable

**Source:** `input_json/ShopLotteryLineupDataTable.json`.

**Row struct (from export):** `Class'GameDataTableRow_ShopLotteryLineup'`

## Fields

Each line is a row field path (nested structs use dot notation; `[]` marks array-of-struct). The literal **Unknown** is the official description placeholder; the bracketed line is an AI guess from the field name only.

- **ContainsCount** — Unknown (AI - Possible description: Inferred from name: contains count. Verify in blueprints or binary if accuracy matters.)
- **ID** — Unknown (AI - Possible description: Primary identifier for this row; used as DataTable row name and for lookups.)
- **Id_ItemSet** — Unknown (AI - Possible description: Foreign key to another DataTable or asset row; suffix suggests item set.)
- **Rariry** — Unknown (AI - Possible description: Inferred from name: rariry. Verify in blueprints or binary if accuracy matters.)
- **Weight** — Unknown (AI - Possible description: Inferred from name: weight. Verify in blueprints or binary if accuracy matters.)
