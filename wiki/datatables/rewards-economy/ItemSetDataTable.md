# ItemSetDataTable

**Source:** `input_json/ItemSetDataTable.json`.

**Row struct (from export):** `Class'GameDataTableRow_ItemSet'`

## Fields

Each line is a row field path (nested structs use dot notation; `[]` marks array-of-struct). The literal **Unknown** is the official description placeholder; the bracketed line is an AI guess from the field name only.

- **ID** — Unknown (AI - Possible description: Primary identifier for this row; used as DataTable row name and for lookups.)
- **Id_Item** — Unknown (AI - Possible description: Foreign key to another DataTable or asset row; suffix suggests item.)
- **Id_ItemText** — Unknown (AI - Possible description: Foreign key to another DataTable or asset row; suffix suggests item text.)
- **Quantity** — Unknown (AI - Possible description: Inferred from name: quantity. Verify in blueprints or binary if accuracy matters.)
