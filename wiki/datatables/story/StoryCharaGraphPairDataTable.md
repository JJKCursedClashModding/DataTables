# StoryCharaGraphPairDataTable

**Source:** `input_json/StoryCharaGraphPairDataTable.json`.

**Row struct (from export):** `Class'GameDataTableRow_StoryCharaGraphPair'`

## Fields

Each line is a row field path (nested structs use dot notation; `[]` marks array-of-struct). The literal **Unknown** is the official description placeholder; the bracketed line is an AI guess from the field name only.

- **EndRelateLevel** — Unknown (AI - Possible description: Tier or level index for scaling or unlock logic.)
- **ID** — Unknown (AI - Possible description: Primary identifier for this row; used as DataTable row name and for lookups.)
- **Id_Character** — Unknown (AI - Possible description: Foreign key to another DataTable or asset row; suffix suggests character.)
- **Id_StoryCharaRelateLevel** — Unknown (AI - Possible description: Foreign key to another DataTable or asset row; suffix suggests story chara relate level.)
- **StartRelateLevel** — Unknown (AI - Possible description: Tier or level index for scaling or unlock logic.)
