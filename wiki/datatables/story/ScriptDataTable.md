# ScriptDataTable

**Source:** `input_json/ScriptDataTable.json`.

**Row struct (from export):** `Class'GameDataTableRow_Script'`

## Fields

Each line is a row field path (nested structs use dot notation; `[]` marks array-of-struct). The literal **Unknown** is the official description placeholder; the bracketed line is an AI guess from the field name only.

- **ID** — Unknown (AI - Possible description: Primary identifier for this row; used as DataTable row name and for lookups.)
- **Id_Bgm** — Unknown (AI - Possible description: Foreign key to another DataTable or asset row; suffix suggests bgm.)
- **Id_Map** — Unknown (AI - Possible description: Foreign key to another DataTable or asset row; suffix suggests map.)
- **ScriptFileName** — Unknown (AI - Possible description: Inferred from name: script file name. Verify in blueprints or binary if accuracy matters.)
