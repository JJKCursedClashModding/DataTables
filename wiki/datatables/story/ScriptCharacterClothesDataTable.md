# ScriptCharacterClothesDataTable

**Source:** `input_json/ScriptCharacterClothesDataTable.json`.

**Row struct (from export):** `Class'GameDataTableRow_ScriptCharacterClothes'`

## Fields

Each line is a row field path (nested structs use dot notation; `[]` marks array-of-struct). The literal **Unknown** is the official description placeholder; the bracketed line is an AI guess from the field name only.

- **ClothesFileName** — Unknown (AI - Possible description: Inferred from name: clothes file name. Verify in blueprints or binary if accuracy matters.)
- **FacialFileName** — Unknown (AI - Possible description: Inferred from name: facial file name. Verify in blueprints or binary if accuracy matters.)
- **ID** — Unknown (AI - Possible description: Primary identifier for this row; used as DataTable row name and for lookups.)
