# MissionLayoutDataTable

**Source:** `input_json/MissionLayoutDataTable.json`.

**Row struct (from export):** `Class'GameDataTableRow_MissionLayout'`

## Fields

Each line is a row field path (nested structs use dot notation; `[]` marks array-of-struct). The literal **Unknown** is the official description placeholder; the bracketed line is an AI guess from the field name only.

- **ID** — Unknown (AI - Possible description: Primary identifier for this row; used as DataTable row name and for lookups.)
- **Location[].X** — Unknown (AI - Possible description: Inferred from name: x. Verify in blueprints or binary if accuracy matters.)
- **Location[].Y** — Unknown (AI - Possible description: Inferred from name: y. Verify in blueprints or binary if accuracy matters.)
- **Location[].Z** — Unknown (AI - Possible description: Inferred from name: z. Verify in blueprints or binary if accuracy matters.)
- **Rotation[].Pitch** — Unknown (AI - Possible description: Rotation pitch in degrees for VFX or facing.)
- **Rotation[].Roll** — Unknown (AI - Possible description: Rotation roll in degrees for VFX or banking.)
- **Rotation[].Yaw** — Unknown (AI - Possible description: Rotation yaw in degrees for VFX or facing.)
