# ChatDataTable

**Source:** `input_json/ChatDataTable.json`.

**Row struct (from export):** `Class'GameDataTableRow_Chat'`

## Fields

Each line is a row field path (nested structs use dot notation; `[]` marks array-of-struct). The literal **Unknown** is the official description placeholder; the bracketed line is an AI guess from the field name only.

- **bUseBattleEnabled** — Unknown (AI - Possible description: Boolean flag: when true, enables or applies use battle enabled.)
- **bUseBattleIntervalEnabled** — Unknown (AI - Possible description: Boolean flag: when true, enables or applies use battle interval enabled.)
- **bUseBattleResultEnabled** — Unknown (AI - Possible description: Boolean flag: when true, enables or applies use battle result enabled.)
- **bUseLobbyEnabled** — Unknown (AI - Possible description: Boolean flag: when true, enables or applies use lobby enabled.)
- **bUseVsEnabled** — Unknown (AI - Possible description: Boolean flag: when true, enables or applies use vs enabled.)
- **ChatCategory** — Unknown (AI - Possible description: Inferred from name: chat category. Verify in blueprints or binary if accuracy matters.)
- **ChatLabel** — Unknown (AI - Possible description: Inferred from name: chat label. Verify in blueprints or binary if accuracy matters.)
- **ID** — Unknown (AI - Possible description: Primary identifier for this row; used as DataTable row name and for lookups.)
