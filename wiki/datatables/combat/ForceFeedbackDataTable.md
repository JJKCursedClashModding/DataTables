# ForceFeedbackDataTable

**Source:** `input_json/ForceFeedbackDataTable.json`.

**Row struct (from export):** `Class'GameDataTableRow_ForceFeedback'`

## Fields

Each line is a row field path (nested structs use dot notation; `[]` marks array-of-struct). The literal **Unknown** is the official description placeholder; the bracketed line is an AI guess from the field name only.

- **Duration** — Unknown (AI - Possible description: Inferred from name: duration. Verify in blueprints or binary if accuracy matters.)
- **ForceFeedbackChannel** — Unknown (AI - Possible description: Inferred from name: force feedback channel. Verify in blueprints or binary if accuracy matters.)
- **ID** — Unknown (AI - Possible description: Primary identifier for this row; used as DataTable row name and for lookups.)
- **Power** — Unknown (AI - Possible description: Inferred from name: power. Verify in blueprints or binary if accuracy matters.)
