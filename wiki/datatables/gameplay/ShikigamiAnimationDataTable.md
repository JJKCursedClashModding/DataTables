# ShikigamiAnimationDataTable

**Source:** `input_json/ShikigamiAnimationDataTable.json`.

**Row struct (from export):** `Class'GameDataTableRow_ShikigamiAnimation'`

## Fields

Each line is a row field path (nested structs use dot notation; `[]` marks array-of-struct). The literal **Unknown** is the official description placeholder; the bracketed line is an AI guess from the field name only.

- **Filename** — Unknown (AI - Possible description: Inferred from name: filename. Verify in blueprints or binary if accuracy matters.)
- **ID** — Unknown (AI - Possible description: Primary identifier for this row; used as DataTable row name and for lookups.)
- **Key** — Unknown (AI - Possible description: Inferred from name: key. Verify in blueprints or binary if accuracy matters.)
