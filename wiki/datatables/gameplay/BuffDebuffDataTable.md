# BuffDebuffDataTable

**Source:** `input_json/BuffDebuffDataTable.json`.

**Row struct (from export):** `Class'GameDataTableRow_BuffDebuff'`

## Fields

Each line is a row field path (nested structs use dot notation; `[]` marks array-of-struct). The literal **Unknown** is the official description placeholder; the bracketed line is an AI guess from the field name only.

- **bEffectEnabled** — Unknown (AI - Possible description: Boolean flag: when true, enables or applies effect enabled.)
- **BuffDebuff** — Unknown (AI - Possible description: Inferred from name: buff debuff. Verify in blueprints or binary if accuracy matters.)
- **FloatValue** — Unknown (AI - Possible description: Inferred from name: float value. Verify in blueprints or binary if accuracy matters.)
- **ID** — Unknown (AI - Possible description: Primary identifier for this row; used as DataTable row name and for lookups.)
