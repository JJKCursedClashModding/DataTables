# PvEMissionWaveLotteryDataTable

**Source:** `input_json/PvEMissionWaveLotteryDataTable.json`.

**Row struct (from export):** `Class'GameDataTableRow_PvEMissionWaveLottery'`

## Fields

Each line is a row field path (nested structs use dot notation; `[]` marks array-of-struct). The literal **Unknown** is the official description placeholder; the bracketed line is an AI guess from the field name only.

- **bBoss** — Unknown (AI - Possible description: Boolean flag: when true, enables or applies boss.)
- **EvaluationMax** — Unknown (AI - Possible description: Inferred from name: evaluation max. Verify in blueprints or binary if accuracy matters.)
- **EvaluationMin** — Unknown (AI - Possible description: Inferred from name: evaluation min. Verify in blueprints or binary if accuracy matters.)
- **ID** — Unknown (AI - Possible description: Primary identifier for this row; used as DataTable row name and for lookups.)
- **Id_MissionWave** — Unknown (AI - Possible description: Foreign key to another DataTable or asset row; suffix suggests mission wave.)
- **Weights** — Unknown (AI - Possible description: Inferred from name: weights. Verify in blueprints or binary if accuracy matters.)
