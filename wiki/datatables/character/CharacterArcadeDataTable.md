# CharacterArcadeDataTable

**Source:** `input_json/CharacterArcadeDataTable.json`.

**Row struct (from export):** `Class'GameDataTableRow_CharacterArcade'`

## Fields

Each line is a row field path (nested structs use dot notation; `[]` marks array-of-struct). The literal **Unknown** is the official description placeholder; the bracketed line is an AI guess from the field name only.

- **ClearFlagConditions** — Unknown (AI - Possible description: Inferred from name: clear flag conditions. Verify in blueprints or binary if accuracy matters.)
- **ID** — Unknown (AI - Possible description: Primary identifier for this row; used as DataTable row name and for lookups.)
- **Id_ArcadeMissionSet** — Unknown (AI - Possible description: Foreign key to another DataTable or asset row; suffix suggests arcade mission set.)
- **Id_MissionCharacter** — Unknown (AI - Possible description: Foreign key to another DataTable or asset row; suffix suggests mission character.)
