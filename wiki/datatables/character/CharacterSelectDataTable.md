# CharacterSelectDataTable

**Source:** `input_json/CharacterSelectDataTable.json`.

**Row struct (from export):** `Class'GameDataTableRow_CharacterSelect'`

## Fields

Each line is a row field path (nested structs use dot notation; `[]` marks array-of-struct). The literal **Unknown** is the official description placeholder; the bracketed line is an AI guess from the field name only.

- **bUseListItemLeftPadding** — Unknown (AI - Possible description: Boolean flag: when true, enables or applies use list item left padding.)
- **ID** — Unknown (AI - Possible description: Primary identifier for this row; used as DataTable row name and for lookups.)
- **PlacementIndex** — Unknown (AI - Possible description: Inferred from name: placement index. Verify in blueprints or binary if accuracy matters.)
- **VisualLobbyPlacementIndex** — Unknown (AI - Possible description: Inferred from name: visual lobby placement index. Verify in blueprints or binary if accuracy matters.)
