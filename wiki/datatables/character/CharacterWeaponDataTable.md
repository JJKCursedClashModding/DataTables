# CharacterWeaponDataTable

**Source:** `input_json/CharacterWeaponDataTable.json`.

**Row struct (from export):** `Class'GameDataTableRow_CharacterWeapon'`

## Fields

Each line is a row field path (nested structs use dot notation; `[]` marks array-of-struct). The literal **Unknown** is the official description placeholder; the bracketed line is an AI guess from the field name only.

- **bDefaultHidden** — Unknown (AI - Possible description: Boolean flag: when true, enables or applies default hidden.)
- **ID** — Unknown (AI - Possible description: Primary identifier for this row; used as DataTable row name and for lookups.)
- **Id_Weapon** — Unknown (AI - Possible description: Foreign key to another DataTable or asset row; suffix suggests weapon.)
- **SocketName** — Unknown (AI - Possible description: Inferred from name: socket name. Verify in blueprints or binary if accuracy matters.)
