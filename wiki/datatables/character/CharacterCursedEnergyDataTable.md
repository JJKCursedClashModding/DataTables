# CharacterCursedEnergyDataTable

**Source:** `input_json/CharacterCursedEnergyDataTable.json`.

**Row struct (from export):** `Class'GameDataTableRow_CharacterCursedEnergy'`

## Fields

Each line is a row field path (nested structs use dot notation; `[]` marks array-of-struct). The literal **Unknown** is the official description placeholder; the bracketed line is an AI guess from the field name only.

- **Add_Rate_Solo** — Unknown (AI - Possible description: Inferred from name: add rate solo. Verify in blueprints or binary if accuracy matters.)
- **ID** — Unknown (AI - Possible description: Primary identifier for this row; used as DataTable row name and for lookups.)
- **LevelThreshold** — Unknown (AI - Possible description: Inferred from name: level threshold. Verify in blueprints or binary if accuracy matters.)
- **Recover_Speed** — Unknown (AI - Possible description: Linear or angular speed in engine units per second.)
- **Recover_Speed_Rate_Solo** — Unknown (AI - Possible description: Inferred from name: recover speed rate solo. Verify in blueprints or binary if accuracy matters.)
- **Recover_Wait** — Unknown (AI - Possible description: Inferred from name: recover wait. Verify in blueprints or binary if accuracy matters.)
- **Recover_Wait_Rate_Solo** — Unknown (AI - Possible description: Inferred from name: recover wait rate solo. Verify in blueprints or binary if accuracy matters.)
