# CharacterMouthPatternDataTable

**Source:** `input_json/CharacterMouthPatternDataTable.json`.

**Row struct (from export):** `Class'GameDataTableRow_CharacterMouthPattern'`

## Fields

Each line is a row field path (nested structs use dot notation; `[]` marks array-of-struct). The literal **Unknown** is the official description placeholder; the bracketed line is an AI guess from the field name only.

- **AnimationTime** — Unknown (AI - Possible description: Duration in seconds unless engine uses normalized time.)
- **EndAnimationTime** — Unknown (AI - Possible description: Duration in seconds unless engine uses normalized time.)
- **ID** — Unknown (AI - Possible description: Primary identifier for this row; used as DataTable row name and for lookups.)
- **MouthTypeArray** — Unknown (AI - Possible description: Inferred from name: mouth type array. Verify in blueprints or binary if accuracy matters.)
