# Wiki

Documentation for DataTable exports: [datatables/](datatables/README.md).

To learn how to edit moves, see the [Moves Reference](reference/moves.md)

For fields that use enums, you need to use the numeric value. Enums will have a `::` in the middle. For example, `EGameAttackTransitionType::InputHit` would need to be replaced with a number (1 if you want the same enum value).